---
interface Props {
  size?: number;
  class?: string;
}

const { size = 32, class: className = '' } = Astro.props;
---

<img
  src="/images/framerschool-icon.png"
  width={size}
  height={size}
  alt=""
  class:list={['pf-logo', className]}
  decoding="async"
/>
