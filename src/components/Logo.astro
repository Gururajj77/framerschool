---
interface Props {
  size?: number;
  class?: string;
}

const { size = 24, class: className = '' } = Astro.props;
---

<img
  src="/images/logo.svg"
  width={size}
  height={size}
  alt=""
  class:list={['pf-logo', className]}
  decoding="async"
/>
