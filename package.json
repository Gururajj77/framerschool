{
  "name": "framerschool-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=framerschool --branch=main"
  },
  "dependencies": {
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
